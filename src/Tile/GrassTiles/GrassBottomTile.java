package Tile.GrassTiles;

import Tile.Tile;
import graphics.Assets;

public class GrassBottomTile extends Tile {
    public GrassBottomTile(int id) {
        super(Assets.dirtArray.get(1), id);
    }
}